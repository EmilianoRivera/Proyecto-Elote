services:
  postgresql:
    image: postgres:15-alpine
    container_name: "eskilokos_postgres_db"
    restart: always
    environment:
      POSTGRES_DB: "SistemaRestaurante"
      POSTGRES_USER: "admin"
      POSTGRES_PASSWORD: "admin"
    ports:
      - "5432:5432"